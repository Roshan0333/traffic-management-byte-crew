import React from 'react';

const Alerts = ({ trafficData }) => {
    return (
        <div className="alerts">
            <h2>Alerts</h2>
            {/* Display alerts based on trafficData */}
        </div>
    );
};

export default Alerts;

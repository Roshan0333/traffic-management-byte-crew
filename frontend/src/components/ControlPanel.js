import React from 'react';

const ControlPanel = () => {
    return (
        <div className="control-panel">
            <h2>Control Panel</h2>
            {/* Control panel elements can be added here */}
        </div>
    );
};

export default ControlPanel;
